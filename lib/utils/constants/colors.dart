import 'package:flutter/material.dart';

class TColors {
  TColors._();
  // Primary Colors
  static const Color primaryColor = Color(0xFF187118);//0xFF156915
  static const Color secondaryColor = Color(0xFF21D500);
  static const Color neutralColor = Color(0xFFFFFFFF);
  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Background Colors
  static const Color backgroundLight = Color(0xFFFCFBFD);

  // Error Colors
  static const Color errors = Color(0xFFFF0000);
}
